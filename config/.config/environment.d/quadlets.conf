# Quadlet global configuration
# This file is loaded by systemd user session and makes variables available to all quadlet containers

# Base path for persistent storage (adjust based on your setup)
QUADLET_STORAGE_PATH=/mnt/minilab-data
