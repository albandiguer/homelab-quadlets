[Unit]
Description=Plane Project Management Pod
After=network-online.target

[Pod]
PodName=plane
# Expose the proxy (which routes to web, api, space, admin, live)
PublishPort=8085:80

[Install]
WantedBy=multi-user.target default.target
