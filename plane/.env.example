# Plane Configuration
# Edit .config/environment.d/plane.conf on the server to configure

# All configuration is managed via environment.d
# See: ~/.config/environment.d/plane.conf

# Required variables:
# - PLANE_DB_USER
# - PLANE_DB_NAME
# - PLANE_DB_PASSWORD
# - PLANE_SECRET_KEY (generate with: openssl rand -hex 32)
# - PLANE_MINIO_ROOT_USER
# - PLANE_MINIO_ROOT_PASSWORD
# - PLANE_WEB_URL
# - PLANE_CORS_ALLOWED_ORIGINS

# Optional email variables:
# - PLANE_EMAIL_HOST
# - PLANE_EMAIL_PORT
# - PLANE_EMAIL_HOST_USER
# - PLANE_EMAIL_HOST_PASSWORD
# - PLANE_EMAIL_USE_TLS
# - PLANE_EMAIL_FROM

# Storage location (set by config service)
# - QUADLET_STORAGE_PATH
